# Track B — Round 4 · Lane B (EXPERIENCE) · Golden Slice KHTN 6 Bài 17

**Date:** 2026-09-05 · **Branch:** `lane-b/round4-experience` → base `integration/round4-2026-09-05`
· **Status:** READY FOR FOUNDER REVIEW — nothing merged.

Founder order (§6, essence): keep UI/UX moving in parallel; Bài 17 at 70–80 % Experience Fidelity;
raise the journey Home → Bookshelf → Book → Chapter → Lesson → Workspace [Đọc][Trực quan][Học với
SAM] → Next Action; priorities Home · Bookshelf/Chapter coherence · Smart Book readability · trust
in a child's words · Visual · SAM Tutor clarity · Next Action · crop/caption defects · withheld
states · source navigation; no machine ids for the child; Nokia validation mandatory.

Boundary unchanged and still machine-held: `ContentTrust`, one-value `EvidencePolicy {none}`,
`WithheldBlock` without a text field, `boundary_test.dart` (throwing store across the journey,
source grep of `lib/features/lesson_workspace/**`). Round 4 adds nothing that can write; it changes
**what the child reads and where machine facts live**, not what the machine asserts.

## 1. What changed, per Founder item

| # | Item | What the child now sees | Where |
|---|---|---|---|
| 1 | **Home = one «Hôm nay» area** | «Chào Na!» → SAM line «Hôm nay mình học Bài 17 · Tách chất khỏi hỗn hợp nhé — bài này SAM đã xếp sẵn ba cách học từ sách. Bấm «Mở bài học» là vào.» (replaces the mic bar «SAM đang học cách trò chuyện» — no chat promised) → label **HÔM NAY** → primary lavender card «BÀI HỌC SAM · BẢN THỬ NGHIỆM» with **«Vì sao bài này? Đây là bài SAM đã xếp sẵn từ sách để con đọc như trong sách, xem sơ đồ / bảng, trả lời 3 câu hỏi trong sách cùng SAM — con thử trước nhé.»** (counted from the document) → secondary white card **«CÒN CÓ THỂ MỞ»** «Có 4 bài để học ở Môn học · Ở Môn học con mở được 4 bài từ sách giáo khoa …» + «Vào Môn học ▸». The contradiction «SAM chưa có bài dạy riêng cho lớp 6» directly under a *Bài học SAM* is gone (only shown when no workspace lesson exists). Order rule: the workspace card is primary unless Toán has an evidence-urgent agenda (review/retrieve) or a TKB book recommendation speaks — then it drops to second, as before. | `lib/features/mission/mission_center_screen.dart` (`samTodayLine`, `workspaceWhyLine`, `_workspaceIsPrimary`) · tests `home_today_area_test`, `home1_test`, `home_workspace_card_test` |
| 2 | **Bookshelf / Book / Chapter coherence** | one vocabulary: shelf tile «55 bài» + **«✨ 1 bài học SAM»** → book «✨ 1 bài học SAM: Bài 17» → chapter row «Bài 16–17 · 2 bài · ✨ 1 bài học SAM · Đã xem (phiên này)» → lesson row «✨ Bài học SAM · 3 cách học · Chưa xem» / **«Đã xem (phiên này): Đọc · Trực quan»** (session trace only — no stars/percent/«đã học», pinned by test). «Mục lục & hoạt động (bản hiện tại)» → «Các bài khác trong sách»; «mở mục lục hiện tại» → «mở trong Môn học». BookScreen now listens to the trace (deferred out of the build phase). | `book_shelf_screen.dart`, `book_screen.dart`, `chapter_screen.dart` · `book_and_chapter_test` |
| 3 | **Smart Book readability** | paragraphs are continuous text like a page (no white card per paragraph — round 3 showed a page as eight loose cards); figure + adjacent captions + source line are **one block** («Hình trong sách · SGK KHTN 6 · trang 60 · chạm hình để xem ảnh gốc»); page chips + «— trang N —» kept; end-of-lesson source line shows «SGK KHTN 6 · trang 60–63» only. **Withheld card**: «Phần này SAM chưa đọc được — con xem SGK trang 61 nhé.» + «Lý do: đoạn này nằm trong sơ đồ» + **«Vì sao SAM để trống?»** → «Chỗ này trong sách là một sơ đồ — chữ nằm xen trong hình. Máy đọc chữ trong sơ đồ hay bị nhầm, mà SAM chỉ chép lại khi chắc là đúng, nên SAM để trống và chỉ trang cho con.» (per reason class: diagram / số & công thức / bảng / chưa rõ loại / default); «Xem ảnh chụp trang sách». **«Tra cứu» sheet** («Sách viết»): «📖 Tra cứu · SGK KHTN 6 · Bài 17 · trang 60», **«Trong mục: Nguyên tắc tách chất»** from the pipeline heading path (never invented; absent on the synthetic fixture), original crop labelled «nguyên gốc (có thể lẫn chữ cạnh hình)», «Xem trong Đọc». | `smart_book_view.dart`, `widgets/withheld_card.dart`, `widgets/source_sheet.dart` · `round4_experience_test` §6.3 |
| 4 | **Trust / source sheet in child language** | «📖 Nói với con»: «Chữ và hình trong bài là chữ và hình của SGK KHTN 6 · trang 60–63 — máy chép lại từ sách, nhớ cả số trang… **«Chưa kiểm định» nghĩa là: chưa có người lớn soát lại từng chữ máy chép, nên có thể còn lỗi nhỏ (một dấu, một chữ). Thấy chỗ nào lạ, con mở sách giấy ra so — số trang ghi ngay bên cạnh.** Chỗ nào máy chưa chắc đọc đúng, SAM để trống và chỉ trang — không đoán.»; ✨ sơ đồ (no rule ids); 🦉 SAM (child runtime line); 📝 «KHÔNG ghi»; **«👨‍👩‍👧 Dành cho bố mẹ»**: internal build, structured verbatim extraction, no false-trust gate (G1) / no licence (D4) ⇒ chip cannot be turned off, nothing recorded as evidence + the six BoundaryClaims. **«🔧 Chi tiết kỹ thuật (dành cho người lớn)»** — closed by default — holds `tsl-enumerated-steps-v1`, runtime refusal codes, key source, pipeline/generator, audit status, source hash, licence. | `widgets/trust_sheet.dart`, `widgets/tech_details.dart` · `round4_experience_test` §6.4, `workspace_screen_test` |
| 5 | **Visual Learning clarity** | legend under the overview strip «Số tím = bước sách viết · số xám = bước SAM để trống (xem trong sách) · chạm một bước để tra cứu lời sách»; 📖 affordance on every step node; comparison hint «Mỗi hàng là một cách sách nêu · chạm một hàng để tra cứu lời sách»; fail-closed copy says why: «SAM chỉ vẽ sơ đồ khi sách viết rõ từng bước hoặc từng cách; bài này chưa có phần như vậy nên SAM không tự vẽ…»; link renamed «ⓘ Nguồn & độ tin». Typed renderers (process, comparison, concept map, timeline) unchanged; Bài 17 still fails closed to Process + Comparison. | `visual_view.dart` · `round4_experience_test` §6.5 |
| 6 | **SAM Tutor clarity** | header runtime line in child words, numbers kept: **«Máy đã kiểm 7/17 bước là lời lấy đúng trong sách (giải thích, câu hỏi, gợi ý, bước tiếp) · 10 bước còn lại là lời viết sẵn để thử (gợi ý, phản hồi, chỉ từng bước).»** — both the counts and the list of step kinds are now derived from the plan (row 12 and §4 D3); legend «Nhãn xanh «runtime có kiểm» = lời lấy đúng trong sách, máy đã kiểm · nhãn tím «kịch bản thử nghiệm» = lời viết sẵn để thử.» (the two label strings are core-owned `SamMode`/`PlannedStepMode.childLabel`, unchanged); **hint ladder** «Bậc gợi ý ① ② · Gợi ý cho tớ ✋» with used rungs filled, hint bubbles captioned «Gợi ý 1/2», end of ladder «SAM đã gợi ý hết rồi — con cứ trả lời thử, SAM sẽ chỉ chỗ trong sách»; SÁCH VIẾT cards say «· chạm để tra cứu»; end card unchanged (participation only). Technical runtime line (`runtimeGuided/prototypeScripted · refusals HINT_UNSOURCED, …`) lives only in the trust-sheet fold. **Lane A-runtime's round-4 APIs have since landed and are consumed** — see §2 and row 12. | `tutor_view.dart`, `widgets/sam_bubble.dart` · `round4_experience_test` §6.6, `tutor_view_test` |
| 7 | **Next Action consistent with the runtime rule** | the «SAM đề xuất» card keeps the runtime's reason (R2–R5 from `NextBestLearningAction`) and adds one line **«Đã mở: ● Đọc ○ Trực quan ○ Học với SAM»** — the session trace the rule reads, so the child sees why the next step is what it is; never evidence, never percent. Reason capped at 6 lines as a frame safety net (real reasons ≤ 3 lines on the Nokia; D-R3-03 respected). | `lesson_workspace_screen.dart` · `round4_experience_test` §6.7 |
| 8 | **Crop / caption defects (display-side)** | `bleedScale = 1.14`: each crop is scaled 14 % inside its aspect-fixed box so ~6 % per edge falls outside the clip — removes the neighbour text line at the top of p60 fig02 / p63 fig02 and the caption bleed under p60 fig03 (D-R3-05) without touching the image or the bbox; the source sheet shows the **untrimmed** crop labelled «nguyên gốc (có thể lẫn chữ cạnh hình)». Captions are still grouped by reading-order adjacency, never by `captionBlockId` (O5). **Reported to Lane A-pipeline** (§5). | `smart_book_view.dart` (`_imageBox`) · `round4_experience_test` §6.3 |
| 9 | **No machine ids child-facing (grep test)** | `no_machine_ids_test.dart` walks Home → Giá sách → Sách → Chương → Vào bài học → Đọc → Trực quan (every shape/instance + Bảng tóm tắt) → Học với SAM (explain / ask / hint) → «Nguồn & độ tin» → «Sách viết» for a paragraph, an image and a withheld block, on the **synthetic and the real fixture**, and asserts no visible `Text` matches `tc2-p1 | sdm-v2 | tsl-…-v1 | toc-ocr | page_feature | math_guard | unknown_role | 0N-sgk-… | :NNN | HINT_UNSOURCED | KEY_NOT_VALIDATED | OVER_CAP | GUARD: | PLAN: | .py | @vN | runtimeGuided | prototypeScripted | sourceBlockId | synthetic: | …enum names`; then opens the fold and asserts the codes are there (moved, not deleted). Findings fixed by this test: withheld source sheet «Mã lý do máy: page_feature:diagram»; trust sheet rule ids, key source «prototype — suy từ đoạn 06-sgk-…:tc2-p1:005», «Nguồn máy: tc2-p1 · tool/corpus/tsl_to_lesson_document.py@v1»; Smart Book end line «… · tc2-p1 / sdm-v2». | `test/features/lesson_workspace/no_machine_ids_test.dart` |
| 10 | **Evidence manifest per iteration** | `tool/evidence/retain.py --round round4 …` → `docs/design/track-b-evidence/round4/MANIFEST.json` (git SHA, APK sha256, packVersion per pack, fixture provenance/trust, device `getprop`, frame sha256s, steps → results; PASS without frame downgraded). Frames also at `~/Desktop/wal-evidence/round4-<iter>-<step>.png`. | §4 |

| 11 | **Two unstamped emitters (Founder §4 fallout)** | nothing visible changed for the child; what changed is what *counts*: an Assessment answer is graded by the registered `fraction-check-v1` and carries its stamp, so it feeds mastery again, and the Home demo domain grades real fraction problems instead of carrying a hand-typed stamp. | `assessment_screen.dart`, `mission_data.dart` · `demo_domain_validation_test` · the two `widget_test` skips lifted |
| 12 | **Quote rule wired + two script defects fixed (Founder §5)** | «Máy đã kiểm **7/17** bước…» (was 5/17) and q1's second hint now carries the green «runtime có kiểm» label; it quotes the book verbatim, names no method and cites no page it invented. | `runtime_plan.dart` (`quoteIndex:`), `tool/corpus/tsl_to_lesson_document.py` (two hint strings) |
| 13 | **Research slice has its own Home area (Lane C × Lane B)** | «SAM ĐANG TẬP ĐỌC SÁCH KHÁC» + «Đây không phải bài của lớp con — SAM đang tập đọc thử một cuốn sách khác. Con xem cho biết cũng được. Bài hôm nay của con ở phía trên nhé.» then the «LÁT CẮT NGHIÊN CỨU · SÁCH LỚP 5» card. Never inside «Hôm nay», never labelled «BÀI HỌC SAM». | `mission_center_screen.dart` (`researchAreaLine`) · `home_research_card_test` |
| 14 | **Four defects the device found** | see §4 — reading headroom, chapter emphasis, a self-contradicting runtime line, a timeline card that said the same thing three times. | §4 |

Not built (doctrine, unchanged from round 3): stars/percent on chapters and progress, «Chính xác! 🎉» + confetti, free chat with SAM (concept-chuong frames 6–7), a bottom tab bar, XP, minutes, a page-by-page SGK viewer.

## 2. Lane A / Lane C consumption

- **Lane A-runtime (round 4, PR #74, merged into this branch):** `EvidenceReadClass`, the strict `replayMastery` / `learningMapStateFor` defaults, `StudentLessonState.standing` + `evidenceNote`, and **`SourceQuoteIndex` (R4.10)**. The contract asked Lane B for one line — `quoteIndex: SourceQuoteIndex.fromLessonDocument(doc)` in `runtime_plan.dart` — and without it the rule was inert. With it wired, the two script fixes below change the measured number. Lane B still constructs no `PlannedStep` and no `EvidenceValidation`.
- **Lane A-runtime's two returned one-liners** are closed (§1 row 11), and the round-3 doctrine allowlist in `validated_evidence_doctrine_test` shrank by both entries. The stamp is **minted by the validator**, never typed: a new Lane B test forbids any `EvidenceValidation(` under `lib/features/**`.
- **Lane C (round 4, PR #75, CI green, merged into this branch):** `timeline_view.dart`, the `visual_view.dart` timeline arm, `workspace_catalog.dart`'s research slot, and the Home `researchLessons` card. One conflict in `mission_center_screen.dart` (Lane C's `researchCardKey` vs this lane's `samLineKey` / `_workspaceIsPrimary`) resolved keeping both. The gitignored real fixture for the slice was installed from Lane C's own sandbox output (`poc-out/round4/lane-c/tc2-lsdl5/v1/history-rules/` + its 23 crops) — nothing was re-run.
- **Lane A-pipeline:** nothing consumed. **The Bài 17 fixture was NOT regenerated** — it is still `tc2-p1`, 73 blocks, 69 trusted / 4 withheld, both process diagrams and the tutor script intact (verified before and after). Only the `tutorScript` object was replaced, with output from this branch's generator on the *current* tc2-p1 TSL, and both fixed quotes were checked against the *current* fixture's own blocks (`p063:tc2-p1:005`, `p064:tc2-p1:006`).

## 3. The five numbers (never averaged)

### 3a. EXPERIENCE FIDELITY vs the concept boards — judgement bands, before → after

Measured frame by frame against `concept/concept-ai-first/learning-view.png` (6 frames: Giá sách → Chọn sách/Chương → Vào bài học → Đọc → Trực quan → Học cùng SAM) and `concept-chuong.png` (9 frames). **The denominator is what the boards ask for minus what doctrine forbids** — stars, %, confetti, «Chính xác! 🎉», free chat and the bottom tab bar are on the boards and are deliberately not built, so they are excluded, not counted as misses. Every «after» band below has device frames behind it (§4); none is a desk judgement.

| Screen / View | Round 3 | Round 4 | Basis (what closes the gap, what is still open) |
|---|---|---|---|
| Home | 50–60 % | **70–80 %** | greeting + SAM line + one primary card with a child-words «why» + secondary «còn có thể mở» + a separate research area ≈ board frame 5 / «Việc SAM đề xuất»; open: no search, no filter chips, no bottom tabs (frames 1–2) |
| Giá sách | 60–70 % | **65–75 %** | cover grid + «55 bài» + «✨ 1 bài học SAM», one vocabulary with book/chapter; open: no search field, no Tiểu học/THCS/Yêu thích filters, no illustrated subject tiles |
| Sách (Book) | 65–75 % | **70–80 %** | cover + «✨ 1 bài học SAM: Bài 17» + chip + Mục lục with the OCR caveat in child words; open: no Giới thiệu/Ghi chú tabs, no per-lesson rows under the chapter (board frame 3 lists lessons inline) |
| Chương | 60–70 % | **75–85 %** | lesson rows read «3 cách học · Chưa xem» / «Đã xem (phiên này): Đọc · Học với SAM», and after this round's fix the row **with** a Bài học SAM is the emphasised one — one colour vocabulary with Home |
| Vào bài học (picker) | 80–90 % | **80–90 %** | matches board frame 3 almost element for element (SAM bubble, three numbered cards with counts, «SAM đề xuất … vì sao?»); unchanged this round |
| Mode 1 Đọc | 70–80 % | **85–90 %** | continuous prose + figure/caption/source as one block + page chips + font control ≈ board frame 4; **this round the page went from 57 % to 73 % of the screen** (measured, §4); open: no table rendering inside Đọc, no «Trước / 3 8 / Tiếp» pager |
| Mode 2 Trực quan | 65–75 % | **70–80 %** | legend + 📖 affordance + tap-to-source + honest fail-closed copy; **open and unchanged: the board's illustrated mindmap with coloured, emoji-bearing nodes does not exist — our nodes are text**. This is now the largest single gap to the boards. |
| Mode 3 Học với SAM | 70–80 % | **80–88 %** | child runtime line whose parenthetical is now measured, label legend, visible hint ladder, SÁCH VIẾT cards, and one hint that genuinely earned the «runtime có kiểm» label; open by doctrine: no praise, no chat box |
| Next Action | 75–85 % | **80–90 %** | reason + «Đã mở ● ○ ○» trace, verified changing across the walk (Đọc → Trực quan → Học với SAM) |
| Nguồn & độ tin (sheet) | (60–70 %) | **80–90 %** | child section + parent section + closed fold; verified on device that the fold holds the ids and the sheet shows none |
| **Lát cắt nghiên cứu** (new) | — | **65–75 %** | own Home area with an honest child line, breadcrumb, timeline with «7 mốc · trang 37 · chữ sách, SAM chỉ xếp lại», story sources with verbatim «(Theo …)», ordering practice with no score; open: proper nouns lower-cased by `titleCase`, visible source tone-mark defects, and its Đọc view has no device frame yet |
| **Overall** | **70–80 %** | **80–85 %** | «1 bài học – 3 cách học – 1 bước tiếp» is legible on every device frame, every child-facing string is child language, and no machine id reaches the child. The band does not go higher because **Trực quan is still text where the board is illustrated**, and the bookshelf/book chrome (search, filters, tabs) is absent. |

### 3b. SOURCE REALITY — visible content elements by `ContentTrust` (Bài 17 real bridge fixture, `capabilityCensus()`)

**97 visible elements**: `trustedStructuredLesson` **78** (69 text/image blocks + 9 semantic items) · `withheld` **4** · `prototype` **5** (tutor script steps) · `fixtureFromTrustedCorpus` **10** (OCR chapter refs) · `fixtureSynthetic` 0 · `trustedCorpus` 0. **Unchanged** by round 4 — this lane changed no content. The LS&ĐL 5 research slice adds 58 blocks (35 trusted / 17 withheld / 6 image) + 1 timeline semantic, counted separately and never merged into the Bài 17 denominator.

### 3c. SOURCE TRUST — elements through a production-trusted path: **0 / 97**

`trustedCorpus` does not exist; TSL is `trustedStructuredLesson`, `auditStatus = sampledNoGate`, licence `internalResearchOnly`. Unchanged.

### 3d. PEDAGOGY REALITY — tutor steps **as shown on the device**

- **Bài 17 (real fixture): 5 / 17 runtimeGuided → 7 / 17.** Read off the Nokia at 21:17 and again at 21:28: «Máy đã kiểm 7/17 bước…». The two that flipped are `q1/hint#1` (fabricated «trang 62» citation removed, quote made contiguous, block `p063:tc2-p1:005`) and `q3/hint#1` (the two dropped «các» restored; its first quote already verified against `p064:tc2-p1:004`). **The number was measured, not set**: `q2/hint#1` still fails `QUOTE_NOT_IN_SOURCE` because the defect is OCR in the source block («khoa»), an A-pipeline fidelity class the runtime must not paper over; the three `#0` hints still fail `HINT_UNSOURCED` because they quote nothing; all three `feedbackMatched` and all three `scaffold` still fail `KEY_NOT_VALIDATED`.
- **LS&ĐL 5 research slice: 0 / 7 runtimeGuided.** The device says so in the child's words — «Máy chưa ràng buộc được bài này với sách — mọi bước là lời viết sẵn để thử» — because no `SemanticBinding` is registered for the slice. Fail-closed, honest, and visible.
- **No validator was registered** for either lesson.

### 3e. EVIDENCE REALITY

Interactions creating validator-permitted evidence: **0 of 0 possible** in the workspace (`validator == null` on every planned step, `EvidencePolicy.none`). Interactions performed on the device across both iterations: 9 mode switches, 2 hint requests, 2 source-sheet opens, 1 tech-fold open, 1 timeline mốc tap, ~30 scrolls — **none of which can create evidence by construction** (`boundary_test` runs the whole journey against a throwing store). Outside the workspace, round 4 *restores* two evidence paths that the strict default had silently switched off: Assessment answers and the Home demo domain now carry a minted `fraction-check-v1` stamp.

## 4. Device loop — Nokia 6.1 (Android 10), «Na · Lớp 6», portrait, WiFi adb `192.168.1.3:5555`

Protocol, both iterations: read-only `dumpsys window` + `dumpsys power` before every input burst; two screencaps ≥ 20 s apart before the first input — **identical bytes both times** (20:55:04 / 20:55:29 and 21:25:20 / 21:25:52), so the Founder was not holding the phone. Never woken, never unlocked, no profile or setting or app data touched, `install -r` only; `com.workizen.tongtai` never touched. Every frame was read before the next tap. Two frames that caught the launcher after a back-key overshoot were deleted immediately.

**Manifest:** `docs/design/track-b-evidence/round4/MANIFEST.json` (32 frames, 30 PASS / 0 FAIL / **0 downgraded** — every claimed frame exists), checklist at `.../steps.json`. Frames also at `~/Desktop/wal-evidence/`.

| Iter | Build | Walked | Defects found → fixed |
|---|---|---|---|
| 1 | `4d870e1` · APK `f0fd3553…` | 24 steps: Home «Hôm nay» → research area → Giá sách → Sách → Chương → picker → Đọc (prose, figure, questions) → Trực quan (legend, steps, «Sách viết» sheet, tech fold) → Học với SAM (runtime line, ask, hint ladder) → **Lane C's queued checklist steps 01–03 + 05** on the LS&ĐL 5 slice | **D1 Đọc had 57 % of the screen** — fixed chrome measured 820 of 1920 px and the page scrolled under the pinned «SAM đề xuất» card. **D2 chapter emphasis inverted** — lavender on the lesson *without* a Bài học SAM. **D3 the runtime line contradicted itself** — «7/17 … (giải thích, câu hỏi, bước tiếp)» while 2 of the 7 were hints; the parenthetical was a hard-coded string. **D4 each timeline mốc said the same thing three times** («40 - 43» / «Hai Bà Trưng» / «Hai Bà Trưng (40 - 43)»). |
| 2 | `746627f` · APK `ca21ef1d…` | 5 re-walk steps on exactly the four changed surfaces | All four verified fixed on the device: reading area **57 % → 73 %**; the row with a Bài học SAM is now the emphasised one (and the session trace propagated to the book's chapter row); the runtime line now reads «(giải thích, câu hỏi, **gợi ý**, bước tiếp) · … (gợi ý, phản hồi, **chỉ từng bước**)»; three mốc now fit where two did. |

**Not walked:** Lane C checklist step 04 (Đọc view of the research slice — its withheld cards and the p41 timeline table). It is covered by `no_machine_ids_test` on the real fixture but has **no device frame**; recorded as `NOT WALKED` in `steps.json` rather than claimed. Lane C step 06 (sessions unchanged) is recorded as a **code claim, not a device claim** — no on-device store inspection was performed.

## 5. Returned to other lanes / Founder (not decided here)

1. **Founder — adopt tc2-p2 for the Bài 17 fixture?** A-pipeline's re-run withholds 9 previously-trusted blocks, which would delete the whole «Học với SAM» view and one Trực quan diagram from this journey (one of the six blocks the script quotes becomes withheld). This lane deliberately did **not** regenerate. Related: the fixture's tutor script hangs off six block ids, so *any* versioned re-run can silently remove a whole learning mode — **the script's coupling to block ids is a fragility worth a decision, not just a fix.**
2. **A-pipeline / Founder — source tone-mark defects are now visible to the child.** Bài 17 Đọc shows «Trong không khi», «lăng xuông»; the LS&ĐL 5 slice shows «Lý bĩ», «Ngô quyền đại phả quân nam hãn». These are exactly the class tc2-p2 would withhold. Today the child reads them as if the book said them.
3. **Lane C / A-pipeline — `LessonDocument.titleCase` lower-cases proper nouns.** «thời kì **b**ắc thuộc», «Lý **b**ĩ», «**n**am hãn». Not fixable from display without inventing a proper-noun list; the printed TOC casing should be preserved instead. Core-owned, so reported rather than patched.
4. **Lane C — the History explain step says «mình thử ba câu»** while the script has four asks (`history-tutor-v1`).
5. **Lane C — the research slice has no `SemanticBinding`**, so Pedagogy Reality for it is 0/7. Honest today; registering one is a Founder / Lane A decision, not a UI one.
6. **A-pipeline (carried from round 3, still present):** crop bbox bleed on `p061:fig02` / `p064:fig02` / `p061:fig03`; caption link `p062:fig05 → :024`; the `SourceRefBlock` text carrying the pipeline tag.
7. **A-runtime — the two core-owned label strings** «runtime có kiểm» / «kịch bản thử nghiệm» are still jargon for an 11-year-old; Lane B added a legend rather than editing them. Proposal unchanged from the draft.
8. **Founder — `pack-option-key-v1`** for Reader/Quiz option keys, unchanged since round 3. Those two emitters are the only entries left in the doctrine allowlist.
9. **Legacy Scale screens are off the round-4 journey but reachable**: GDTC 6 lists seven rows all numbered «Bài 1» with different titles, each repeating «SAM đang học bài này — con chụp bài tập để học cùng nhé». Seen on device while navigating; not in §6 scope.

## 6. Tests

`flutter analyze` clean · `flutter test` **961 passed / 1 skipped / 0 failed** (the skip is the pack-conditional one, present in the baseline too). Baseline on the integration base: 896 / 1.

New this round: `test/features/mission/demo_domain_validation_test.dart` (validator-minted demo stamps + no `EvidenceValidation(` under `lib/features/**`); the research-slice walk and the research-area coherence cases added to `no_machine_ids_test` and `home_research_card_test`; the emphasis assertion in `book_and_chapter_test`; the non-repetition assertions in `timeline_view_test`. Updated: `pedagogy_runtime_test`'s real-fixture measurement (5/17 → 7/17, with each remaining refusal code still pinned to the specific defect that causes it) — an A-runtime file this lane had to move because the measurement moved; flagged here and in the PR.

## 7. How to run

```
cp <main>/assets/pack/{lesson-index-g*.json,*.png,sam-units.db,sam-stories.db} assets/pack/ && cp <main>/assets/pack/covers/* assets/pack/covers/
cp <main>/assets/fixtures/real/*.json assets/fixtures/real/ && cp <main>/assets/fixtures/real/crops/*.png assets/fixtures/real/crops/   # gitignored; do NOT regenerate the Bài 17 one (§5.1)
flutter analyze && flutter test && flutter build apk --debug
python3 tool/evidence/retain.py --round round4 --frames docs/design/track-b-evidence/round4 \
  --steps docs/design/track-b-evidence/round4/steps.json --apk build/app/outputs/flutter-apk/app-debug.apk \
  --packs assets/pack --fixture assets/fixtures/real/lesson-06-sgk-khoa-hoc-tu-nhien-6-b17.json \
  --adb $ANDROID_HOME/platform-tools/adb --device 192.168.1.3:5555 --print
```
