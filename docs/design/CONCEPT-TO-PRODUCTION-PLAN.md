# CONCEPT → PRODUCTION PLAN (Task Order §21.J, §22)

**Lợi thế bất ngờ của audit:** first slice KHÔNG phải build-from-scratch — kernel đã
có TutorSession/Confirm/Agenda/feedbackFor/TeachingProvenance/eval-guard. Slice ≈ NỐI
+ RENDER.

## P0 — FIRST VERTICAL SLICE (chứng minh end-to-end)
Home(home1, 5 intent, header switcher) → Subject Home(07) → Camera(08)→Confirm(09 —
đã build logic) → Tutor Start(10 provenance card) → Diagnostic-lite(11: probe, không
điểm) → Workspace(12: T1 + gõ đáp án; handwriting = P2 Canvas) → Hint(13 engine-
ladder) → Your Turn(14) → Success(15 feedbackFor) → Why(16)/Source(17) → Review(18
reviewStateOf). Acceptance: curriculum-grounded (position từ structure.json thật) ·
camera-safe (Confirm bắt buộc) · pedagogically-constrained (BCNN không thể xuất hiện
— test) · provenance visible từng act · assistance tracked (lineage) · evidence
generated (store) · next action từ resolveAgenda. Đơn vị demo: Toán 5 B6 (corpus sâu nhất).

## P1 — CORE LEARNING
Quiz(20) · Assessment split(21a/b/22) · Learning Map(19 claim-based) · Sessions(30) ·
Progress(31 educational metrics) · Multi-profile switcher đầy đủ (§26) · Notifications
learning-only(37) · Settings(38 child-safe).

## P2 — SUBJECT SPECIALIZATION
TV surfaces đầy đủ (23) · Essay=Compose full (24) · KHTN experiment surface · Sử
timeline+source · Địa map · AI theo QĐ2422 (29 replace) · Canvas handwriting (§27 POC).

## P3 — PARENT
32/33/34 sau khi P0-P1 sinh evidence thật đáng xem; Parent PIN; consent flows.

## P4 — ADVANCED/OPTIONAL
Voice full (35) · Library 5-khu (36) · Simulation POC (25/26) · Teacher phase.

Nguyên tắc xuyên suốt: mỗi màn P0 phải đọc DATA THẬT (structure/pack/store) — không
mock; de-gamify pass áp toàn bộ (XP/streak/rank OUT, heatmap/tiến-bộ-của-mình IN).
