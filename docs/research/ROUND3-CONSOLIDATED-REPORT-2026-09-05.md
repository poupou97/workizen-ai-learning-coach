# HỌC CÙNG SAM — Round 3 Consolidated Founder Report · 2026-09-05 · PROVE + EXPERIENCE + DISCOVER
**READY FOR FOUNDER REVIEW — nothing merged.** Integration base PR #68 now = main + round-2 PRs + round-3 lane PRs (#69 runtime, #70 research, #71 experience, #72 bridge + audit); `flutter analyze` clean; `flutter test` **896 passed / 1 skipped / 0 failed** with packs + bridge fixture; Python tool tests OK. Denominators per D5 (3,679 canonical historical; 3,381 ranged; 238 TC-v2 repaired-ranged; population of shipped blocks 3,334 / activities 2,798).

## LANE A — PROVE
**Trusted pipeline delta (A1, PR #72):** one real bridge `tool/corpus/tsl_to_lesson_document.py` (TSL → LessonDocument, deterministic, 100 % provenance preserved per block: id, book, pdf/printed page, bbox, role+confidence, relations, extraction/OCR/agreement, trust, withholding reasons; boundary + pipelineVersion + auditStatus). Trust mapping fail-closed on both sides: TSL TRUSTED → `trustedStructuredLesson` (explicitly NOT production truth; chip «nguồn SGK có cấu trúc, chưa kiểm định (nội bộ)»; removal needs G1 thresholds AND licence D4 — two gates); withheld/conflict → `WithheldBlock` with no text field; unknown roles → withheld `unknown_role:*`. Golden slice from the bridge: **73 blocks = 69 trustedStructuredLesson + 4 withheld, 0 synthetic**.
**False-trust status (A2, P0 — MEASURED, no thresholds set):** 484/484 rows annotated (single AI annotator, page-render based, pre-checks disclosed). Shipped content (420 blocks / 398 activities):
- display fidelity error **0.698** [0.652, 0.740] · teaching-critical error **0.505** [0.447, 0.564] (of 277 applicable) · reading-order error 0.306 · role error 0.093 · lesson-attachment error 0.028 (8/11 = back covers on a book's last lesson)
- **false trust (derived) 0.710** [0.664, 0.751] = teaching-critical **0.333** [0.290, 0.380] + display-only 0.293 + other 0.083; annotator-verdict false trust 0.382; narrowest reading (numbers/formulas/units only) 0.183
- by subject: **Toán FT 0.878 (tc 0.519)** · Tiếng Việt 0.587 (tc 0.168) · KHTN 0.299 (tc 0.065); two-column pages 0.804 vs single 0.528; all 11 attachment errors and 77/99 order errors on two-column pages
- **KHTN 6 Bài 17 TSL stratum (60 blocks): teaching-critical 0/60 [0, 0.060], display 12/60 (tone-mark non-words, circled numerals), order 0/22, attachment 0/4; the 4 withheld regions are safe over-rejections**
- a «< 1 %» claim needs n ≥ 381 with 0 failures — impossible at the observed shipped rates; the failing classes must be fixed and re-sampled; TSL pipeline needs ≥ 381 judged blocks with 0 failures.
- Consequence stated plainly: **the currently shipped Scale content (113/111 lessons, sam-units) is not trustworthy for teaching** — 1 in 3 served blocks has a teaching-critical error; Toán worst. The TSL path on the golden lesson has 0 teaching-critical errors on 60 blocks. This is evidence contradicting the "113 proven" assumption; continuing to ship it as-is would manufacture progress → Founder decision required (stop condition E).
**Evidence integrity (A4):** #60 kept; torn-line/rewrite/retry regressions added; options note for append-on-disk durability and hash-chain integrity (no storage change).
**Evidence contract (A3):** `ValidatedEvidence` mintable only by a closed registry of deterministic validators (`fraction-check-v1` competence; `candidate-gate-v1` participation); TutorSession refuses to grade without a registered validator; map state / BKT / parent line refuse rogue stamps; strict mode implemented, **not default** (legacy graded history would lose «Tự làm được» — Founder call).
**Lineage (A5):** camera-originated Tutor = null/null asserted; stamping only from a resolved `LearningContext` with lesson identity.
**Convergence (A6):** `SemanticBinding` contract + registry with exactly ONE binding (Bài 17 «Học với SAM» → declared concept; methods = declared ∩ TutorScope ∩ sourceStated with page; fails closed otherwise); no runtime added; no mass conversion.
**Pedagogy runtime delta (A7):** `PedagogyRuntime.planForScript` turns the typed TutorScript into `PlannedStep`s through the existing guard; per-step mode visible in the UI; on Bài 17: **5 of 12 steps runtime-guided** (explain, verbatim SGK asks, next), 7 prototype (hints unsourced; feedback/scaffold depend on a prototype key). The guard caught **3 answer leaks** in the prototype hints — rewritten in the single generation path; zero leaks pinned by test.
**Student State / Next Action delta (A8):** `StudentLessonState.fromEvents` + `NextBestLearningAction.forLesson` rules R0–R5 in the Founder order (Đọc → Trực quan → Học với SAM → next lesson only from validated success); no minutes/percent/mastery invented; wired into the workspace.

## LANE B — EXPERIENCE (PR #71)
Latest Nokia journey: Nokia 6.1 / Android 10, learner «Na · Lớp 6», pack `g6-20260905T0437Z-07a24504`; iteration 1 full journey (26 frames, build `3409527`), iteration 2 (build `08af1bf` incl. #69) 7 frames until the Founder picked up the phone; evidence manifest with git SHA / APK sha256 / packVersion / device getprop / frame hashes: `docs/design/track-b-evidence/round3/MANIFEST.json` (33 frames, 29 PASS / 6 SKIP / 1 OBSERVED, 0 downgraded). Integrated-build walk (iteration 3, coordinator, 16:19–16:21): Home honest card + «BÀI HỌC SAM · BẢN THỬ NGHIỆM» card → «Mở bài học» → picker (breadcrumb, trust chip ⓘ, three cards with real counts, «SAM đề xuất cách này — vì sao?» reason from state) → Học với SAM (header states **«Runtime kiểm được 5/17 bước … 12 bước còn lại là kịch bản thử nghiệm»**, first step labelled «SAM (runtime có kiểm)» with a SÁCH VIẾT quote) → Trực quan tab. 5 frames, manifest `docs/design/track-b-evidence/round3-iter3/MANIFEST.json` (git SHA, APK sha256, packVersions, fixture provenance, device getprop, frame hashes).
Major UX changes: breadcrumb + first-open «Vào bài học» picker with per-view counts and «SAM đề xuất cách này — vì sao?»; trust sheet «Nguồn & độ tin» (trust in child words, rule ids, runtime line, six boundary claims); Smart Book page chips/dividers, figure rows with grouped captions, badges; Visual shape tabs + concept-map and timeline renderers (typed only; Bài 17 fails closed to Process + Comparison); tutor phase strip, «Câu n/N», per-step «SAM (runtime có kiểm)» / «SAM (kịch bản thử nghiệm)»; Home «BÀI HỌC SAM · BẢN THỬ NGHIỆM» card; child-facing names instead of raw ids; app label «Học cùng SAM»; branded launch window (cold-start blank fixed); MapReader grade from the book id; gallery narrowed to the open book.
Defects found on device: raw book id on «Bạn có biết?», clipped proposal reason, machine code on withheld card, sixth tutor phase off-screen, clipped anchor, white launch window — all fixed (one re-verified on device, the rest by tests pending iteration 3); observed pipeline issues: crop bleed, caption-like paragraph role (TSL).

## LANE C — DISCOVER (PR #70)
Learned: the Bài 17 document layer generalises across Science (238/238 TSLs), the semantic layer does not (Process 68/238, Comparison 6/238, tutor script 1/238); tables have no cells (0/20), 28 % of question regions are withheld (figure-dependent), 118 footnote/activity/option blocks drop silently — fail-closed is honest but wide. Per family: History/Geography needs prose-dated timeline + source/attribution shapes (best non-Science trust: gold p041 FTR 0); Math fails closed at the source (56 % formula pages; role vocabulary wrong: HĐ/Ví dụ/Giải/RULE); Vietnamese/Literature cleanest pages, weakest lesson identity (TV5 TOC +2 pages); Informatics has the strongest SGV signal and the first real data table; English/primary 1–3 image-first only. Assumption ledger: **PROVEN 5 · FALSIFIED 14 · UNTESTED 4**. Contradictions reported: gold lesson numbers on LS&ĐL 5 vs printed headers; timeline task classified DRAW_CREATE; units counters drift.
**Candidate second golden lesson: LS&ĐL 5 · Bài 8 «Đấu tranh giành độc lập thời kì Bắc thuộc» (pp. 36–39)** — attacks 12/22 assumptions (prose-dated `TimelineSemantic`, source + attribution block, a deterministic `TimelineValidator`, header-vs-TOC identity, no «Em đã học»), is the only non-Science candidate with a measured trust number inside its pages, has SGV section keys and an existing Surface family (SourceReader). Needs before becoming a slice (Founder gate): bounded tc-v2 run on LS&ĐL 5, second human read of gold p041/p080, three PROPOSED rules, a grade-5 persona decision. Alternatives: Toán 6 Bài 9 → TV5 Bài 3 → Tin học 6 Bài 1.

## FIVE SCORES (never averaged)
| Score | Value | Basis |
|---|---|---|
| **Experience Fidelity** | **70–80 %** overall (Vào bài học 80–90 · Đọc 70–80 · Trực quan 65–75 · Học với SAM 70–80 · Next Action 75–85 · Sách 65–75 · Chương 60–70 · Giá sách 60–70 · Home 50–60) — up from 55–65 % | frame-by-frame vs the two concept boards, Lane B §3a |
| **Source Reality** | 92 / 97 visible elements are real SGK content (bridge fixture from the TC-v2 TSL, internal); 5 prototype; 0 synthetic | machine census `round3_metrics_test` |
| **Source Trust** | **0 / 97** through a production-trusted path (TSL is `trustedStructuredLesson`, audit `sampledNoGate`; G1 thresholds + licence not decided) — while the golden lesson's TSL measured 0/60 teaching-critical errors | bridge + audit |
| **Pedagogy Reality** | **5 / 17** tutor steps runtime-guided on the integrated build (explain, verbatim SGK asks, next); 12 prototype — as displayed on the device (Lane B measured 5/12 on the pre-bridge fixture) | `PedagogyRuntime` on the bridge fixture; frame round3-3-04 |
| **Evidence Reality** | **0** interactions create validator-permitted evidence (no registered validator for Bài 17 free text; Sessions unchanged on device); validators exist only for the Deep fraction path | A3 registry, Lane B device check |
Also: device verified? — yes: iterations 1–2 (Lane B, 33 frames, manifest) + iteration 3 on the integrated build (5 frames, manifest) · fixture/prototype dependency — yes (chip on every screen; 7/12 tutor steps; TOC chapters) · open P0 — false-trust thresholds (G1), licence (D4), strict-validation default · regressions — none (896/896; khoaExperiments diff documented in #61) · next falsifier — LS&ĐL 5 Bài 8.

## AUTONOMY STATUS PER SCOPE
| Scope | Status | Why |
|---|---|---|
| Core engineering (evidence, integrity, lineage, provenance, runtime contracts) | **B — READY WITH GUARDRAILS** | contracts + doctrine tests exist; PRs only; strict-validation default and storage policy remain Founder calls |
| UI/UX (golden slice, Views, defects) | **B — READY WITH GUARDRAILS** | boundary types + throwing-store test + device loop with manifest; no evidence emission; fixture chip mandatory |
| Architecture research (Lane C) | **A — READY** | docs-only, evidence-labelled, falsification-oriented |
| Trusted-content expansion | **C — NOT READY** | shipped-content false trust 0.71 (tc 0.33); no thresholds; TSL path unproven beyond 60 blocks (needs ≥ 381 at 0) |
| Pedagogy automation | **C — NOT READY** (bounded runtime B for the golden slice only) | 5/12 steps real; hints/feedback need sourced material or validators; no SGV keys |
| LLM Tutor | **C — NOT READY** | no client; cage optional at type level; WAL-30 gate |
| Public content distribution | **C — NOT READY** | D4 licence undecided; everything internal-only |

## Decisions requested
1. G1 thresholds per rate (display / teaching-critical / order / role / attachment / false trust) and the second annotator slice (≥ 10 %). 2. What to do with the shipped Scale content now that it measures 0.71 false trust — keep behind the internal chip, withhold Toán, or freeze to Deep + TSL-backed lessons only. 3. Strict validation as default (legacy history loses «Tự làm được»). 4. Second golden lesson: LS&ĐL 5 Bài 8 (gate items in Lane C 03 §4). 5. Licence D4. 6. Review order: #68 (integration) then lane PRs #69 → #71 → #72 → #70, or squash the integration as one.
