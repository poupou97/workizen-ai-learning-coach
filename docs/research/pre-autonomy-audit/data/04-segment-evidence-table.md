# Data 04 — Runtime chain, segment by segment (Layer C)

Chain: Source → SDM/source extraction → Trusted Structured Lesson → Learning Surface → Pedagogy Runtime → SAM realization → Learner Action → Evidence → Student State → Next Action. Statuses: PROVEN (code + test + real data at runtime) · PARTIAL · MISSING · BROKEN · RESEARCH ONLY. Labels as in 03.

| # | Segment | Status | Code path (or absence) | Test that proves it | Notes |
|---|---|---|---|---|---|
| 1 | **Source** (SGK/SGV PDF → OCR lines) | RESEARCH ONLY / PROVEN-OFFLINE (Python, not in app) | `tool/ingest/ocr_worker.sh`, `batch_pipeline.sh`, `build_registry.py`, `build_structure.py`; outputs under `poc-out/` (gitignored; present on this machine: `poc-out/ocr`, `graph`, `units*`); manifests `docs/ingest-manifests/grade-*.json` | none in `test/` (no Dart touches PDFs) | Provenance carried as book id + pdf page + printed page only. Licence: `ContentLicense.localResearchOnly` declared (`knowledge_content_provider.dart:18`) but not enforced at runtime (no provider implementation). |
| 2 | **SDM / source extraction** | RESEARCH ONLY | main: `tool/corpus/tc_sdm.py` (TC-v1), `layout_extract.py` (WAL-206 XY-cut), `tool/ui/pattern_router.py` (WAL-204), `tool/extract/*`; TC-v2 (`tc2_sdm.py`, `tc2_attach.py`, `tc2_tsl.py`) exist **only** on `research/tc-v2-science-slice` (0 `tc2_*` files on main) | none in `test/`; Python gates `tool/extract/verify_corpus_gates.py`, `tool/corpus/content_quality_gate.py` (not run here) | Branch `01-FOUNDER-SUMMARY`: chain DONE offline on 1,049 pages; FTR 0.10–0.12 on hard pages; QUESTION precision 0.83–0.89 (< 0.95 gate). None of it reaches Dart. |
| 3 | **Trusted Structured Lesson** | MISSING in app (nearest = `LessonIndex` pack, PARTIAL) | `tool/ui/build_lesson_index.py` → `assets/pack/lesson-index-g{N}.json` → `LessonIndex.fromJsonString` `lib/features/subjects/lesson_index.dart:390-624` (drops records missing passage/attribution/steps/provenance) → `loadForGrade` `:627` (null if asset missing) | TEST-PASSED: `lesson_index_test` (fixture; FILE THẬT case ran with packs), `data_driven_lesson_test`, `lesson_duplicates_test`, `book_shelf_test`, `architecture_gate_test` (2nd subject + 2nd grade open, pack-gated — passed with packs), `source_gallery_test` | No block id, trust status, role confidence or pipeline version in Dart (branch `F.1`, `G.1`). Lesson identity `LessonKey` (`slice_curriculum.dart:77`) is exact-match, fail-closed. |
| 4 | **Learning Surface** | PROVEN (6 reachable surfaces) · 1 built-but-unrouted | Reader `reader_screen.dart:30`, Compose `compose_lite_screen.dart:39`, Experiment `experiment_screen.dart:27`, SourceReader `source_reader.dart:56`, MapReader `map_reader_screen.dart:23`, Tutor `tutor_screen.dart:23`, Assessment `assessment_screen.dart:38`, SourceGallery. Routing `subject_home_screen.dart:643-666` (`_activityAction`). `QuizSelectScreen` (`quiz_select_screen.dart:26`) has **0 constructions in `lib/`** — unreachable. `resolveSurface`/`resolvePresentation` 0 callers | TEST-PASSED widget suites: `reader_screen_test` (12), `compose_lite_screen_test` (8), `experiment_screen_test` (7), `source_reader_test` (7), `map_reader_test` (3), `tutor_screen_test`, `assessment_test`, `quiz_select_screen_test` (7, but screen unrouted) | "shortText" ⇒ unsupported (fail closed, `learning_activity.dart:96`; check C4c). |
| 5 | **Pedagogy Runtime** | PARTIAL (deterministic rules run; the "runtime" types do not) | Live: `decide()` `adaptive_engine.dart:89-162` (rule-based diagnosis → `TutorScope`), `TutorSession` ladder `tutor_session.dart:148-179` (±1, REVEAL gate `:77-78`), `AssistancePolicy` rules `assistance_policy.dart:37` (assessment: cap none). Dormant: `LearningExperienceBlueprint`/`blueprintViolations`, `PedagogicalPattern`, `PlannedAct`, `RealizationRequest`/`validateRealization` — 0 callers in `lib/features`+`main.dart` (grep) | TEST-PASSED: `tutor_session_test` (7), `thin_slice_test` (6), `learning_blueprint_test` (blueprint validates a real TutorSession log — offline), `realization_contract_test` (ACT_OVER_RUNG fail-closed), `subject_validation_test`, `challenge_policy_test` | `decide` at runtime always receives the hard-coded case `denominator-non-divisible` for Home/Assessment (`mission_data.dart:264`, `slice_flow.dart:375`); real case only on the camera/exercise path (`slice_flow.dart:138`). |
| 6 | **SAM realization** | PROVEN deterministic · LLM SHADOW ONLY | `hintTextFor` `tutor_session.dart:205-215` (templates from `MethodHints`, slots from `SolvableProblem`), `feedbackFor` `tutor_feedback.dart:53` (banned-praise list `:47`), `sourceLineForChildOf` `teaching_provenance.dart:57`, per-surface fixed strings. LLM: `buildTutorPrompt` `tutor_prompt.dart:39` (0 callers in `lib/`), `validateTutorOutput` `output_guard.dart:42`, harness `tool/shadow/{run_shadow.py,eval_shadow.py,gen_prompts.dart,guard_check.dart}` calling `claude -p` offline | TEST-PASSED: `method_hints_test`, `tutor_feedback_test`, `tutor_prompt_test` (null outside scope), `output_guard_test` (real shadow transcripts), `citation_guard_test`, `scenario_bank_test` S1–S9 | `pubspec.yaml` has no HTTP/LLM dependency; `grep -i anthropic\|openai\|http\.\|dio\|socket lib` → comments only. `SAM-GENERATIVE-SHADOW-RESULTS.md`: KEEP SHADOW (DOC-CLAIM consistent with code). |
| 7 | **Learner Action** | PROVEN | `TutorSession.submit/requestHint` `tutor_session.dart:115-179`; surface handlers `_pick/_askHint/_answeredOpen` (`reader_screen.dart:104-141`), `_submitDraft/_submitRevision` (`compose_lite_screen.dart:109-136`), `_finish` (`experiment_screen.dart:74-99`), `_conclude` (`source_reader.dart:102-125`), `_submit` (`assessment_screen.dart:74-104`) | widget suites above; `tutor_session_test:90,104` (selfCorrection rules) | Every action is one event, no double count within a session (`tutor_session.dart:114-141`). |
| 8 | **Evidence** | PROVEN with integrity caveats | mint sites (10) → `recordSession` `session_recorder.dart:25-52` (empty ⇒ no session; assess violations returned) → `LearnerStore.appendSession` → `JsonlLearnerStore` `learner_store.dart:79` (in-memory append-only lines) → `FileLearnerStore._flush` `file_store.dart:29-32` (**whole file rewritten** each mutation) | TEST-PASSED: `session_recorder_test` (3), `learner_store_test` (§5–§7, 14), `file_store_test` (3), `evidence_lineage_test`, `replay_audit_test` (incl. `:98` uniqueness for `attributeEvidence`) | Script: **C1 FAIL** (TutorSession eventIds collide across re-open of the same exercise), **C3 FAIL** (same session appended twice ⇒ evidenceCount doubles), **C9** (hand-edited JSONL replayed undetected). Lineage fields absent on 4 of 5 surfaces (C7). |
| 9 | **Student State** | PARTIAL | Deep: `masteryFromStore` `slice_flow.dart:38-48` → `replayMastery` `evidence_weighting.dart:156` → `ConceptSummary.of` `concept_summary.dart:250` → `explainConcept` `parent_explanation.dart:52`; `reviewStateOf` `review_schedule.dart:86`; `challengeSignalFor`. Scale: `learningMapStateFor` `learning_map_state.dart:33` → Subject Home badge (`subject_home_screen.dart:200`), `recentLessonTouches` (`parent_session_summary.dart:37`) | TEST-PASSED: `mastery_test`, `concept_summary_golden_test` (21), `evidence_replay_test`, `review_schedule_test`, `learning_map_state_test` (7), `parent_session_summary_test`, `knowledge_state_screen_test`, `progress_screen_test` | Deep state covers 2 cases/1 concept; Scale state sees only experiment events (C7); the two never meet. `LearningMapScreen` (WAL-142) uses the Deep state, the Subject Home badge (WAL-181) uses the Scale state — same "map", two truths. |
| 10 | **Next Action** | PARTIAL | `resolveAgenda` `learning_agenda.dart:233-292` over `[AgendaConceptInput]` of length 1 (`mission_data.dart:247-260`); `decide` for the card title (`:262-269`); `nextBookRecommendation` `next_lesson.dart:47-92` (timetable-only); `proposeIntent` `learning_intent.dart:85-123` per lesson; UI merge `mission_center_screen.dart:228-248`; result screen `KnowledgeStateScreen` (`slice_flow.dart:358`) | TEST-PASSED: `learning_agenda_test` (rest first-class, in-stage only), `next_lesson_test`, `learning_intent_test` (`⭐⭐ KHÔNG có căn cứ ⇒ KHÔNG đề nghị`), `slice_flow_test:158` (tutor → store → Home changes), `home1_test`, `profile_switcher_test` | For grade ≠ 5 the agenda is never computed (`mission_data.dart:206-230`). No action reads Scale-path evidence except "studiedToday" session count. |

## Emitter inventory (who mints `LearningEvent`, through which gate, with which lineage)

| Emitter | Gate | `correct` | `support` | `sourceDocumentId/lessonNo` | `lookup` respected | `skillCaseId` registered? |
|---|---|---|---|---|---|---|
| `tutor_session.dart:90` | direct | bool | ladder | **none** | n/a (no context) | yes (2 cases) |
| `evidence_validator.dart:67` (Experiment) | validator | null | from claim (kind ignores it, C6) | from context | yes | no (`khtn-thi-nghiem`) |
| `reader_screen.dart:81` | direct | bool or null | hint/none | **none** | yes (`:80`) | no |
| `compose_lite_screen.dart:91` | direct | null | hint/none | **none** | yes (`:90`) | no |
| `source_reader.dart:107` | direct | null | none | **none** | yes (`:106`) | no |
| `map_reader_screen.dart:44` | direct | null | none | **none** on the event (`:114` `sourceDocumentId: m.book` belongs to the `SourceAsset` crop, not the `LearningEvent`) | **no** (no context param) | no |
| `assessment_screen.dart:84` | direct | bool | none | **none** | n/a | falls back to `'unknown-case'` |
| `quiz_select_screen.dart:61` | direct | bool or null | hint/none | none | no | — (screen unrouted) |
| `multi_skill_diagnosis.dart:45` | core helper | bool | param | none | n/a | no feature caller |
| `mission_data.dart:100` | demo fixture | true | none | none | n/a | fixture only |
