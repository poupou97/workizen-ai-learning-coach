# 09 — Recommended next round (for Founder decision; nothing started)

**Round name:** GATES BEFORE GROWTH — make what already ships provable, then unlock.

**Founder decisions needed first (blocking):** D1 evidence contract for ungraded self-reports (`07` #1); D2 lock the six architecture forks or record F-d "keep today's shape" (`03` §B.6); D3 false-trust audit protocol + acceptance bar for shipped lessons (`02` G1); D4 licence position for verbatim SGK text and page images (`07` #8); D5 denominators after TOC repair (ranged ≈ 3,425 for six books; canonical ≥ 3,725 proposed); D6 mark the `K12-CONVERGENCE-CENSUS.md` approval sentence `TBD` or attach the approval.

**Autonomous work that can start on D1–D3 being *pending* (B-lane, PRs only):** `08` "safest tasks" 1–5, plus the Role-Layer research bound from TC-v2 doc 06 (icon/colour signal; ≥ 300 science gold questions) — research output only.

**Explicitly not in the next round (Founder orders in force):** full-corpus reprocess; 27-pattern expansion; Short-Answer Surface; mass Learning Views implementation; finalising LearningContext ↔ LearningView; assuming page-image licensing; any LLM wiring.

**Exit criteria for the round:** the measurable list in `08` "Success criteria"; a re-run of this checkpoint's scripts (`scripts/a_layers.py`, `b_113_join.py`, `runtime_checks.dart`, `pack_counts_test.dart`) showing C1/C3 PASS, 0 router entries in a default build, and lineage coverage 100 % — reported with denominators, never as "+N lessons".
