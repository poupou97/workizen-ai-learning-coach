# 07 — Top 10 gaps (ranked) · Founder pre-autonomy checkpoint · 2026-09-05

Ranking rule: **P0** = autonomous building is dangerous until handled · **P1** = strong product impact · **P2** = polish / scale / later. Evidence paths are relative to `poc-out/audit/pre-autonomy/` unless a repo path is given. "Agent may fix?" = may an autonomous agent land it as a PR without a Founder decision (the PR itself still stops at READY FOR FOUNDER REVIEW).

| # | Pri | Gap | Evidence | Impact | Recommended action | Blast radius | Agent may fix? |
|---|---|---|---|---|---|---|---|
| 1 | **P0** | **Self-reports become competence claims.** Four ungraded taps ("Con đã trả lời xong", map done, Sử stance, Compose draft) mint `independentAttempt`; the validator ignores `support`; `LearningMapState` shows 🔵 "Tự làm được" and the parent line says "Con đã tự làm được Bài N". | `04` C.2(4), checks C6/C6b; `reader_screen.dart:104-109`, `map_reader_screen.dart:41-55`, `source_reader.dart:107`, `evidence_validator.dart:70`, `learning_map_state.dart:41-46`, `parent_session_summary.dart:70` | A parent is told a competence that no evidence supports — violates F1/F3 doctrine at the UI layer while BKT stays honest (two truths) | Founder decides the evidence contract for ungraded activities (self-report ≠ independent attempt); then one mandatory gate for all emitters | All 5 Scale surfaces, Subject Home badge, Parent area | **No** — doctrine decision first; mechanical after |
| 2 | **P0** | **Shipped text is untrusted and unmeasured.** All 113 baseline activities + `sam-units.db` come from the naive extractor TC-v1 measured at false-trust 0.321 on hard pages; 77 % of the 113's pages are hard; where TC-v2 overlaps, 21/32 lessons contain text TC-v2 withholds and 3/32 are attached to the wrong lesson. No false-trust audit exists on the exact blocks served. | `02` §2, §4, §6 gates G1–G3; `data/113-join.md`, `data/tc-metrics.json` | Child reads wrong/mis-attributed textbook text presented as SGK; "113 proven" is wiring, not truth | Gate G1 (statistical false-trust audit on shipped lessons), G2 (capped/header attachment in `build_lesson_index.py`), G3 (lesson-identity check) before any coverage expansion | Every shipped Reader/Experiment/Source activity | Partly — G2/G3 are mechanical (PR); G1 needs the audit protocol approved (Founder) |
| 3 | **P0** | **No build provenance.** `assets/pack` is gitignored; the packs on this Mac are the WAL-206 *variant* build (97 router entries / 76 experiment-only lessons); any APK built here — including the one on the Nokia — ships experimental content indistinguishably from a default build. | `02` §1 row LEARNABLE-variant; `data/coordinator-notes.md`; verified 10:3x | Experimental, human-review-failed content (1/5 exact-scope invalid) can reach a child; regression oracle covers `khoaExperiments` only | Pack manifest (build flags, git SHA, hash) embedded in the pack and asserted by a test; default build regenerated before any device claim | Every APK | **Yes** (mechanical, test-backed) |
| 4 | **P0** | **Evidence store integrity.** `eventId` collides on re-open (C1 FAIL); `appendSession` not idempotent — mastery doubles on a retried finish (C3 FAIL); `FileLearnerStore` rewrites the whole file per mutation; no tamper detection (C9). | `04` C.2(6), `data/04-runtime-checks.md`; `tutor_session.dart:88`, `learner_store.dart:138`, `file_store.dart:29-32` | Double-counted mastery; a mid-write crash loses a learner's whole history; edited files change parent claims silently | Unique ids + idempotent append (agent); append-on-disk + integrity policy (Founder: privacy/cost) | Every learner history, export/delete guarantees | Partly — ids/idempotency **yes**; storage/integrity policy **no** |
| 5 | **P0** | **Two architectures, zero convergence code, unverifiable approval.** Deep path = 1 registered lesson; Scale path = 187 openable lessons; `SemanticBinding` absent; unregistered `skillCaseId`s incl. `'unknown-case'`; a Toán-5 `knowledgeModelVersion` stamped on Tiếng Việt evidence; the "Founder-approved convergence" sentence in `K12-CONVERGENCE-CENSUS.md` has no approval artefact anywhere (WAL-196 Ready, 0 comments). | `03` §B.3, §B.5, §B.6; `data/03-two-paths-inventory.md`; JQL read-only | An agent extending either path deepens the fork and may believe convergence is authorised | Founder locks the six forks in `03` §B.6 (evidence contract, lineage, state function, activity model, runtime containment F-a…F-d, WAL-196 truth) | Every new lesson / subject / surface | **No** — architecture decisions; agent may only mark `TBD` |
| 6 | **P1** | **Lineage gap — docs say wired, code is not.** Only Experiment events carry `sourceDocumentId/lessonNo`; Reader/Compose/Source/Tutor/Assessment do not, so the Learning Map says "Chưa học" for lessons the child did and the parent summary omits them. | `04` check C7; WAL-178/179/181 claims vs grep | "ONE EVIDENCE TRUTH" is false today; Next Action cannot read Scale evidence | Thread `LearningContext` into every emitter + one test per surface; decide the Tutor stamp for camera problems | Learning Map, Parent view, Next Action | **Yes** mechanically (PR for review); Tutor stamp = decision |
| 7 | **P1** | **Role Layer cannot ask safely.** QUESTION precision 0.83–0.89 (< 0.95), ANSWER recall 0.40, ACTIVITY 0.00; no role concept exists in Dart — the app asks whatever the pack builder emitted as a question (XY-cut labeller precision 0.69 for router readings). | `02` §5; TC-v2 `gold-scores.json` | Any graded/Short-Answer prompt would be unsafe; router readings' questions are unverified | Keep Short-Answer deferred (as ordered); build icon/colour signal + ≥ 300 science gold questions before re-measuring | Every question surface | Research yes (bounded); Short-Answer **no** |
| 8 | **P1** | **Licence contradiction in the running app.** `ContentLicense.localResearchOnly` says verbatim SGK must not be shown to end users; Reader/Source/Experiment show SGK text verbatim; page-image delivery is an open legal gate (J.1) but text has the same gap with no gate. | `04` C.2(2); `knowledge_content_provider.dart:16-18`; branch doc J | Legal exposure the moment a build leaves the Founder's devices | Founder/legal decision on text and image delivery; until then, internal builds only | Every Surface | **No** |
| 9 | **P1** | **Pedagogy runtime is a façade; LLM cage optional.** Blueprints/patterns/`PlannedAct`/`validateRealization` have 0 runtime callers; `decide` runs with a hard-coded case; nothing forces model text through the guard; no LLM client exists (good) but nothing type-level prevents wiring one around the cage. | `04` C.2(7), C.3 #5; grep | Docs/Jira overstate "built and guarded"; an agent could wire an LLM with all tests green | Keep WAL-30 gate; propose a `RealizedText` type that only the guard can construct (safe to propose, not to bundle with wiring) | Every future tutor act | Proposal yes; wiring **no** |
| 10 | **P1/P2** | **UI/UX vs concept** — see `05-UIUX-CONCEPT-GAP.md`: Lesson Workspace with three Views does not exist; lesson chooser shows identical "Đọc bài" labels; Home says "SAM chưa có nội dung lớp 6" while KHTN 6 has routed lessons; no withheld/trust-warning state; no device evidence retained. | `05`, `~/Desktop/wal-evidence/audit-*.png` | Product looks like "reader + experiments", not the approved concept | Do not mass-implement Views (as ordered); fix the two honest defects (chooser labels, Home card) as bounded PRs; design the withheld/uncertain state | Home, Subject Home, chooser | Bounded fixes **yes**; Views **no** |

Also recorded (not ranked): denominator/TOC repair adoption (3,381 → ≈3,425 ranged for six books; canonical ≥ 3,725 proposed) — Founder decision; device evidence retention (all prior screenshots gone from the Desktop 2026-09-05 ~10:10; `02` gate G6) — process; the `K12-CONVERGENCE-CENSUS.md` approval sentence must be marked `TBD` per CLAUDE.md rule 5 — governance.
