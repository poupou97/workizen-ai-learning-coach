# Round 4 — PROVE + EXPERIENCE + DISCOVER + LEGACY REPROCESS · plan and ownership (2026-09-05)

Base: `main` @ 61dbfdb (round 2 + 3 merged, Founder-authorized). Lane PRs target `integration/round4-2026-09-05`; nothing merges without explicit Founder authorization.

| Lane | Scope (Founder order §) | Owns | Never touches |
|---|---|---|---|
| A-pipeline (PROVE) | §3 failure classes from the false-trust audit: two-column reading order, role classification, lesson attachment, formula/number/unit fidelity, figure/caption relation, identity/provenance; re-measure with the existing scorer after each fix; no threshold changes | `tool/corpus/**` (pipeline: layout_extract, tc_*, tc2_*, tsl_to_lesson_document, guards, scorers), `tool/tests/**`, `docs/research/PIPELINE-*.md` | `lib/**`, `tool/corpus/legacy/**` (Lane D), `docs/design/**` |
| A-runtime (PROVE) | §4 strict validation default (history preserved, legacy stays unvalidated), §5 grow real pedagogy-runtime coverage of Bài 17 (no hard-coding, no fake validators), Next Action | `lib/core/**` except `lesson_model/**`, `lib/features/tutor/tutor_session.dart`, `test/core/**`, `docs/architecture/**` | `lib/features/**` (B), `lib/core/lesson_model/**` (A-pipeline may extend via the bridge contract only), `tool/**` |
| B (EXPERIENCE) | §6 Home → Bookshelf → Book → Chapter → Lesson → Workspace [Đọc][Trực quan][Học với SAM] → Next Action; readability, child-language trust explanations, withheld states, source navigation, crop/caption defects; no machine ids; Nokia loop + §15 evidence retention | `lib/features/**` (except tutor_session.dart), `lib/app/**`, `android/**`, `assets/fixtures/**`, `test/features/**`, `docs/design/**`, `tool/evidence/**` | `lib/core/**`, `tool/corpus/**` |
| C (DISCOVER) | §7 Golden Slice #2 gate for LS&ĐL 5 Bài 8: bounded TC-v2 run on LS&ĐL 5, independent second review, header-vs-TOC conflicts, proposed semantic/runtime rules (prose-dated TimelineSemantic, source/attribution, TimelineValidator), provenance; build the slice only if the gate passes (typed model additions via the bridge contract, coordinated) | `docs/research/lane-c/**`, `tool/research/**`, `poc-out/round4/lane-c/` (data); if gate passes: `lib/core/lesson_model/timeline_*.dart`, `lib/features/lesson_workspace/views/timeline_*` (announced in the PR) | production packs, `tool/corpus/**` pipeline code (request fixes from A-pipeline) |
| D (LEGACY REPROCESS) | §8–§11 small-batch OLD → NEW reprocess from original source, old-vs-new compare with provenance/version/hash, independent audit incl. the ≥10 % second annotation on high-risk strata, scoreboard; no mass reprocess; legacy never used as trusted teaching source; nothing deleted | `tool/corpus/legacy/**`, `poc-out/round4/legacy/`, `docs/research/legacy-reprocess/**` | pipeline code (uses A-pipeline's as-is, re-runs when A-pipeline ships), `lib/**` |

Shared rules: D4 verbatim SGK internal/research only; denominators per D5; REPROCESSED ≠ TRUSTED; SELF-REPORT ≠ COMPETENCE; camera lineage null/null by default; every device claim has a manifest; five product scores + legacy scoreboard reported separately.
