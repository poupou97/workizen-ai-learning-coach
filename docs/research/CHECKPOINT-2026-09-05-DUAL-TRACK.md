# HỌC CÙNG SAM — Dual-Track Checkpoint · 2026-09-05 (B-lane + Track B) · READY FOR FOUNDER REVIEW — nothing merged

Baseline for every claim: pre-autonomy audit (PR #59, verdict C). Denominators per D5: 3,679 = historical canonical (census 2026-08, curriculum-structure.json); 3,381 = ranged; 238 = TC-v2 repaired-ranged (six Science books). Every number below is MEASURED in a PR's tests/logs unless marked.

## A. CORE PROGRESS (Track A — 7 PRs, all CI green, none merged)

| Founder question | Answer | Evidence |
|---|---|---|
| **DATA — Can we trust what currently ships?** | **Not yet as "trusted"; now MEASURABLE and cleaner.** Default packs g1–12 regenerated with a provenance manifest (schema 1, git SHA, flags, contentHash, packVersion), `experimental=false`, **0 router entries** (was 97); attachment capped + header-repaired + lesson-identity gated: 42/50 khoaExperiments byte-identical, 2 moved to their true lesson, 5 withheld, 2 non-canonical TV5 keys dropped → **learnable 111 / 3,679**. Served text still comes from the naive extractor (TC-v1 false-trust 0.321 on hard pages); the statistical false-trust audit is built (484-row stratified sample of the exact shipped blocks, Bài 17 first; scorer reproduces TC-v2 gold exactly; n ≥ 381 for a <1 % claim) but **not yet annotated** — that is the gate G1 and a reviewer task. Trusted Structured Lessons remain 238 / 238 (offline, Science only). | #61, #65; `poc-out/b-lane/pack-regeneration-report.md`; `docs/research/FALSE-TRUST-AUDIT-PROTOCOL.md` |
| **EVIDENCE — Can a tap still become a competence claim?** | **No.** `EvidenceKind.participation` minted by all four self-report taps, ungraded picks and the validator; 🔵 "Tự làm được" only from validated independent success; parent line for participation = "Con đã học Bài N — con tự báo đã làm xong; SAM không chấm phần này"; legacy `independentAttempt+correct==null` read as participation; BKT unchanged; doctrine test scans `lib/`. | #63 `self_report_doctrine_test.dart`, `learning_map_state_test`, `parent_session_summary_test` |
| **INTEGRITY — Can retry/reopen double-count mastery?** | **No.** Event ids `<exerciseId>@<sessionToken>#<seq>` unique across re-open (7 surfaces); `appendSession` idempotent by sessionId (second append = no-op); old data loads unchanged. Still Founder policy: whole-file rewrite on flush, no tamper hash. | #60 `evidence_integrity_test.dart` (C1/C2/C3 ported), `evidence_id_uniqueness_test.dart` |
| **PROVENANCE — Can every APK prove exactly which pack it contains?** | **Pack: yes. APK: half.** Every pack self-describes and is verifiable (`pack_provenance.py verify`, CI step, hash); the app parser fails closed on router content unless the pack declares `experimental`; `default_build_guard_test` asserts the on-disk packs are default (16/16 with regenerated packs). Not yet: an in-app "about" surface showing packVersion/hash, and a build step binding the APK id to the pack hashes. | #61, #64 |
| **LINEAGE — Can every learner event identify its lesson/source?** | **Every Scale surface: yes** (Reader/Compose/Source/Map/Assessment/Tutor-from-pack stamp `sourceDocumentId/lessonNo`; `diaMaps[].lesson` now emitted by the builder; Learning Map shows "Đã học" after a completed lesson — tested on KHTN 6 Bài 17). **Camera-originated Tutor sessions: null** (returned for Founder decision). | #63 `scale_lineage_test.dart`; #61 |
| **ARCHITECTURE — Is Deep ↔ Scale convergence mechanically possible without a third architecture?** | **Yes, as bounded contracts — no new runtime created.** 14 PROPOSED contracts (participation kind, lineage fields, `buildProvenance`/`packVersion`, `LearningContext.knowledgeModelVersion`, default-build guard…) plus Track B's typed `LessonDocument`/`SemanticData`/`TutorScript` as the candidate TSL → LearningActivity shape. `SemanticBinding` still absent; the six forks (audit 03 §B.6) remain Founder decisions; "Founder-approved convergence" is now marked TBD / PROPOSED (D6). | #63 `docs/architecture/B-LANE-CONTRACTS-2026-09-05.md`; #62; #66 |
| **UI — Have known misleading states been removed?** | **The two named ones: yes.** Home card now says "Có N bài để học ở Môn học" with the real count and "Bắt đầu" goes to the shelf, never the camera; reading rows are distinct ("Đọc bài · trang N — <question…>"). Still open: raw document ids in provenance lines, Toán-6 gallery showing a Toán-5 asset, `android:label="learning_coach"`, grade "5" hard-coded in MapReader. | #64 `home_scale_only_card_test.dart`, `reading_chooser_labels_test.dart`; audit 05 |

Role Layer (research, D3): deterministic icon/box signal lifts QUESTION precision to 0.938 held-out / 0.929 science / 0.857 Bài 17 and ACTIVITY from 0.00 to ≈0.3; **Short-Answer gate (≥ 0.95) NOT met**, not forced (#65).

## B. VISIBLE PRODUCT PROGRESS (Track B — PR #66, CI green, real device, none merged)

Journey built and walked on the Nokia («Na · Lớp 6», 4 builds / 4 iterations, 75 frames): Bookshelf → **Book** (real cover, Mục lục by Chương) → **Chương IV** (rows with "Chưa xem / Đã xem") → **Bài 17 · Tách chất khỏi hỗn hợp** (SGK KHTN 6 tr. 60–63) → **Lesson Workspace** with [📖 Đọc] [✨ Trực quan] [🦉 Học với SAM] + "SAM đề xuất" (reason-bearing, no invented minutes). Smart Book: reading order, figure crops with captions and page source, question boxes, **honest withheld cards** ("Phần này SAM chưa đọc được — con xem SGK trang 61 nhé · Lý do: sơ đồ") with an internal crop. Visual: two Process diagrams (incl. a withheld step) + Comparison table + Bảng tóm tắt, node → source. Học với SAM: labelled **"SAM (kịch bản thử nghiệm)"**, explain → ask → answer → ≤2 hints → check → feedback quoting "SÁCH VIẾT … trang 62" → next; participation-only end card. Every workspace screen carries the non-dismissable chip "Bản thử nghiệm · nội dung nội bộ (từ SGK, chưa phát hành)" (D4).

**Boundary (machine-readable):** `ContentTrust`, `EvidencePolicy.none`, `SamMode.prototypeScripted`, `WithheldBlock` with no text field; boundary test runs the whole journey against a throwing fake `LearnerStore`; **Sessions list unchanged after four full journeys** (frame `trackb-4-19`). Real fixture (verbatim SGK) is gitignored; only a `[MẪU]` synthetic fixture is committed.

**Two percentages (never combined):**
- **EXPERIENCE FIDELITY vs the concept boards: 55–65 % overall** (mode picker 65–75 · Đọc 55–65 · Trực quan 50–60 · Học với SAM 55–65 · Book/Chapter 55–65 · shelf 60–70) — up from 5–15 % for the Learning-View concept at the morning audit.
- **REAL CAPABILITY RATIO (98 visible elements, machine census):** content — 94 % verbatim real SGK text/regions, all delivered as `fixtureFromTrustedCorpus` (TC-v2 TSL, offline, internal), **0 % from a production-trusted pipeline**; structure/behaviour — 26 % prototype rules/script (semantic data, chapter grouping, tutor steps, next-action rule); pedagogy runtime — **0 %**; learning evidence — **0 events**.

Founder's five questions — answered with frames: where (`trackb-3-03→3-06→4-01`), which lesson (`3-05`, `3-06`), three ways (`3-07…3-15`, `4-02…4-12`), what SAM does (`3-07`, `3-16`, `4-03…4-11`), what next (`3-07`, `3-16`, `4-12→4-13`). Defects: 9 found on device and fixed within the iterations; open: cold-start blank frame (pre-existing), TOC chapter titles keep two OCR errors verbatim, one figure→caption mislink in the TSL.

## C. New autonomy verdict: **B — READY WITH GUARDRAILS** (for bounded work in both tracks; still **C for coverage expansion and anything touching trust thresholds**)

Why B now: the five P0 gaps of the morning audit have mechanical answers in open PRs (evidence contract, integrity, provenance, lineage, honest UI) and the boundary is enforced by tests; Track B can grow without creating learning truth. Guardrails that keep it B and not A: (1) the false-trust audit sample must be annotated before any coverage claim or expansion (G1); (2) all PRs stop at READY FOR FOUNDER REVIEW; (3) Short-Answer, LLM wiring, page/text distribution, mass Views, 27 patterns, corpus reprocess stay Founder gates; (4) any parent-facing wording change returns for review.

## D. Decisions requested (none blocking Track B)
1. Review/merge order: #60 → #63 → #64 (stacked), #61, #62, #65, #66, #59 (audit). 2. Annotator + acceptance thresholds for the 484-row false-trust sample. 3. Camera-originated Tutor lineage stamp. 4. Validator claims are always participation — accept or define `ValidatedEvidence`. 5. TV5 TOC +2-page offset repair (G9) and the map asset registry (Bài 1 vs range Bài 2). 6. Track B "Needs from Track A" (trusted `LessonDocument` producer, chapter refs from curriculum-structure, Home catalog awareness, `anchorBlockId`).

## E. Process
All work by three parallel agents in separate worktrees on disjoint files; coordinator verified CI, file lists (no packs, no real fixtures committed), the regenerated packs against the Dart guard (16/16), and six device frames. Nothing merged; WAL-210 → Code Review. Desktop: this file + `HOC-CUNG-SAM-CHECKPOINT-DUAL-TRACK-<ts>.zip` (track-a docs, track-b docs, 75 device frames).
