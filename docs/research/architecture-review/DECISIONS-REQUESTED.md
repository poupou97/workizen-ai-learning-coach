# Decisions requested from the Founder (each with options and the evidence behind it)

Nothing here was decided by the agent. Items 1–4 are filed as PROPOSED in `workizen-knowledge-base` (branch `proposed/tc-v2-source-architecture`, `decisions/DECISIONS.md` + `canonical/05_DECISIONS.md`, D-135…D-138) per `canonical/KNOWLEDGE_UPDATE_PROTOCOL.md`; the rest are product/legal choices for this repo.

| # | decision | options | evidence (MEASURED unless marked) | recommendation (agent's, not binding) |
|---|---|---|---|---|
| 1 | **SDM-as-source** (D-135): the Structured Document Model — not Markdown, not `units-*` — is the source layer; Markdown/units/packs are projections carrying block ids | (a) accept · (b) keep units as source and add trust flags · (c) defer | every stage of the slice consumed and produced SDM blocks; the reason codes, bbox, roles and provenance have no representation in units/Markdown (A.1, TC-11) | (a) |
| 2 | **Block-level trust** (D-136): trust is a per-block status with reason codes from an agreement cascade + deterministic guards; page-level gates retired | (a) accept · (b) keep page gate as an extra guard · (c) defer | slice 0.854 trusted with reasons on the rest; XY-cut page gate trusted 3/50 Toán 5 pages in TC-13; FTR on hard pages 0.10–0.12 either way (A.2) | (a), with (b)'s marginal-cut signal kept as `box_boundary`/`agree_order` reasons (already the case) |
| 3 | **Image-first for math/visual — degraded to "book-first" until J.1** (D-137): formulas, tables, diagrams, maps, picture pages are never extracted as text; they are references to the page (and crops only if licensed) | (a) accept with the `no_images` default · (b) accept image-first assuming J.1 opens · (c) attempt formula OCR | math guard + figure_text + page_feature withheld 42 + 199 + 111 blocks on the slice; TC-09 flattened every fraction in every stack; `no_images` projection is functional (C.2) | (a) — (b) assumes a legal answer that does not exist |
| 4 | **Trusted subset, not whole corpus** (D-138): SAM ships from lessons that carry trusted blocks + withheld references; "3,679 lessons from the books" is not the product statement | (a) accept · (b) keep the whole-corpus thesis and reprocess · (c) defer | 2/238 Science lessons FULL, 236 PARTIAL; full reprocess not authorised and would carry the same 0.10–0.12 hard-page FTR (A.6, I.4) | (a) |
| 5 | **Denominators and TOC repair**: keep 3,679 (canonical) and 3,381 (ranged) as two numbers **and** adopt header-detected lesson counts for the PARTIAL/truncated books | (a) adopt repair → ranged ≈ 3,425 (six books) and canonical ≥ 3,725 (KHTN 7 → ≥ 39, KHTN 8 → ≥ 47), re-derived corpus-wide (H7) · (b) keep the file as is and report "repaired" separately · (c) adopt only the ranged repair | headers detected with 0–2 rejections per book, 53/55 TOC-confirmed on KHTN 6, 22/23 gold attachment (A.4, I.1) | (a) after H7 runs on all books |
| 6 | **Release bar** (WAL-207 OQ11 / J.4): one corpus-wide bar (text ≥ 99.5 %, FTR < 0.1 %, 0 CTE) or per-feature gates + audit on shipped lessons | (a) per-feature gates now (prose/questions/sidebars on the Science family), math/diagram never as text, statistical audit on served blocks · (b) corpus-wide bar (nothing ships) · (c) defer | dev/held-out FTR 0.112/0.121 stable; prose ≈ 0; 54 pages cannot certify < 1 % (TC-17 #2) | (a) |
| 7 | **Short-Answer Surface stays deferred** — confirm, and set what unlocks it | (a) confirm; unlock = QUESTION ≥ 0.95 and ACTIVITY ≥ 0.8 on ≥ 300 science gold questions via an icon/box detector (H2, H3) · (b) unlock on trusted-question precision alone (0.970, n=33) · (c) defer indefinitely | ROLE-LAYER-AND-SHORT-ANSWER-GATE.md §3 | (a) |
| 8 | **Runtime semantics** (F.4): F-a View inside LearningContext · F-b View as sibling node · F-c LearningSession container · F-d keep today's shape until a TSL reader exists | — | F.1 (nothing per-block in Dart), F.3 (readable ≠ askable is a source property) | none — kept OPEN as ordered; the first Dart step (TSL reader, `no_images`) is identical under every option (H4) |
| 9 | **Page-image / crop delivery** (J.1) — route to legal; not a decision the agent or this review can make | (a) legal review of `docs/research/TEXTBOOK-LICENSING-QUESTIONS.md` · (b) declare `no_images` the only mode for the beta | both modes built, counts identical (C.2) | (b) for the beta, (a) in parallel |
| 10 | **Next bounded step** (H) | H1 + H3 (records + gold) · H2 (icon detector) · H4 (TSL reader) · H5 (audit) · H6 (SGV pairing) · H7 (denominators) · none | H | H1 + H3 |

**Not requested (already ordered, obeyed):** no full-corpus reprocess, no 27-pattern expansion, no Learning Views implementation, no merge.
