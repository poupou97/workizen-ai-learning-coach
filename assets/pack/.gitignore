*.db
lesson-index-*.json
# WAL-167 — bìa SGK trích từ PDF: cùng luật WAL-43, KHÔNG commit.
covers/*
*.webp
# ...nhưng THƯ MỤC phải tồn tại trong git: pubspec khai báo `assets/pack/covers/`
# và `flutter analyze` báo lỗi nếu thư mục đã khai báo không có thật. Git không
# theo dõi thư mục rỗng ⇒ giữ một file mốc. (CI đỏ đúng chỗ này, WAL-167.)
!covers/.gitkeep
