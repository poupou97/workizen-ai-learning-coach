# assets/fixtures — Track B Lesson Workspace fixtures (WAL-210)

**Boundary (Founder 2026-09-05):** everything here is FIXTURE or PROTOTYPE.
`MOCK ≠ EVIDENCE · FIXTURE ≠ TRUSTED CORPUS · UI COMPLETION ≠ MASTERY · TAP ≠
COMPETENCE · PROTOTYPE SAM ≠ PROVEN PEDAGOGY · SCREEN EXISTS ≠ CAPABILITY PROVEN`
— machine-readable in `lib/core/lesson_model/content_trust.dart` (`BoundaryClaim`).

| Dir | In git? | Trust value | What |
|---|---|---|---|
| `real/` | **NO** (`.gitignore`, only `.gitkeep`) | `fixtureFromTrustedCorpus` | `lesson-<book>-b<N>.json` + `crops/*.png` generated by `tool/fixtures/make_lesson_fixture.py` from one Trusted Structured Lesson (`tc2-p1`) + SGK PDF page crops. Verbatim SGK text and page crops: **internal / research only (Founder D4)** — never distributed, never committed. |
| `synthetic/` | yes | `fixtureSynthetic` | `lesson-<book>-b<N>.synthetic.json` + `crops/*.png`: clearly fake, paraphrased text (every paragraph starts with `[MẪU]`) and generated placeholder images. Exists so CI and widget tests run on a clean clone. |

The app (`WorkspaceCatalog`) loads `real/` if present, else `synthetic/`. Whenever the
loaded document's trust is not `trustedCorpus` (today: always), every workspace screen shows a
non-dismissable chip «Bản thử nghiệm · …». No `trustedCorpus` document exists yet — the value is
reserved for the future TrustedLearningSource pipeline (TC-10/TC-11).

Regenerate the real fixture on a machine that has `poc-out/`:

```
python3 tool/fixtures/make_lesson_fixture.py            # KHTN 6 Bài 17 (default)
```

Never edit the TSL input (`poc-out/trusted-corpus/tc-v2/tc2-p1/lessons/...`); the generator is
read-only on it and does not "fix" OCR text. Withheld blocks are emitted **without text**.
